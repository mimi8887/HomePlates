<h2><%= @category_name %> Recipes</h2>
<ul>
  <% @recipes.each do |recipe| %>
    <li>
      <h3><%= link_to recipe["recipeTitle"], recipe_path(recipe["recipeId"]) %></h3>
      <p><%= recipe["recipeDescription"] %></p>
    </li>
  <% end %>
</ul>
<%= link_to "Back to Top Recipes", root_path %>
